export default function TopLogo({ logo }) {
    return (
        <div className="top-logo">
            <img src={ logo } alt="logo-zaprecall" />
            <h2>ZapRecall</h2>
        </div>
    );
}